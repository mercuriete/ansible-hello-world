name: ansible-hello-world-devcontainer
services:
  devcontainer:
    build:
      context: ..
      dockerfile: ./.devcontainer/Dockerfile
    environment:
      EXAMPLE: ${EXAMPLE}

    volumes:
      - ..:/workspace:cached
